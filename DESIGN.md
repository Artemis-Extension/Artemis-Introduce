# Design — ARTEMIS 소개 사이트

<!-- 스택: Nuxt 4 · Vue 3.5 · 정적 생성. 경로는 전부 이 저장소 기준.
     제품 UI(콘솔)의 디자인 원본은 본 저장소 apps/web/app/assets/css/main.css 다.
     여기 값은 그 토큰을 그대로 옮기되, 마케팅 표면에만 필요한 것(디스플레이 크기 · 구간 지면 · 별자리)을 덧붙였다.
     둘이 충돌하면 콘솔이 우선한다. -->

## 1. 지배 개념 — 프롬프트가 놓인 자리가 지면을 정한다

이 페이지는 한 프롬프트의 일생이다. 스크롤 진행이 곧 프롬프트의 상태이고,
그 상태가 **지면 색과 배경 별자리의 단계를 함께** 정한다. 다크/라이트는 취향으로 고른 값이 아니다.

    복사본  흩어짐  draft │ v1    v2    staging │ production  run   eval  아직 │ 닫는 말
    ■밤     ■밤     ■밤   │ □밝음 □밝음 □밝음    │ ■밤         ■밤   ■밤   ■밤  │ ■밤
    └ 점이 흩어졌다 모인다 ┘ └ 캔버스 정지 ────────┘ └ 라벨 · 채널 · 빛의 방향 ─────┘

- **밤** = 사람이 찾지 못하거나(사본) 사람이 보지 않는 곳에서 돈다(운영). 별자리가 비친다.
- **밝은 지면** = 사람이 읽고 고치는 곳(구조 · 버전 · 배포 판정). 불투명하고, 렌더 루프를 세운다.

상태 표기(`stamp`)는 코드에 있는 값을 쓴다 — `draft`(Lifecycle), `v1`(PromptVersion), `staging` · `production`(LabelName).
구간 경계는 칼자국으로 남기지 않는다. `[data-from]` 이 앞 구간 색에서 200px 에 걸쳐 시작하고,
`.station--rise` / `.station--fade` 가 별자리와 불투명 지면 사이를 잇는다.

## 2. 색

### 원시 토큰
콘솔의 토스 계열 값을 그대로 옮겼다. 값은 `app/assets/css/main.css` 에 산다.
브랜드 `--blue-50 #3182f6` 은 **채움 전용**이다. 흰 바탕 작은 글자에서 3.7:1 이라, 밝은 지면의 글자는 `--blue-70 #1858c4` 를 쓴다.

### 구간별 지면
| 토큰 | 값 | 구간 |
|---|---|---|
| `--st-top` | `#0a0e14` | 복사본 |
| `--st-where` | `#0c1118` | 흩어짐 |
| `--st-wedge` | `#10161f` | draft |
| `--st-anatomy` | `#f9fafb` | v1 |
| `--st-immutable` | `#ffffff` | v2 |
| `--st-gate` | `#f2f4f6` | staging |
| `--st-deploy` | `#111822` | production |
| `--st-delivery` | `#0e141c` | run |
| `--st-eval` | `#0b1118` | eval |
| `--st-later` | `#090d13` | 아직 |

밤 구간은 지면을 70% 만 칠해 별자리를 비친다. 글자 대비는 왼쪽에서 오는 고정 스크림(`.scrim`)이 지킨다.

### 밴드 의미 토큰
`[data-band="dark"]` 는 `:root` 에도 건다. **없으면 `body { color: var(--ink) }` 가 무효값이 되어
색을 명시하지 않은 모든 글자가 검정으로 계산된다.**

| | dark | light |
|---|---|---|
| `--ink` | `#eef2f7` | `#191f28` |
| `--ink-2` | `#aeb8c6` | `#4e5968` |
| `--ink-3` | `#8793a4` | `#646e7b` |
| `--brand` (글자) | `#7db3fd` | `#1858c4` |
| `--ok` · `--no` · `--warn` | `#5fd9a6` · `#ff9098` · `#f0b35a` | `#047a52` · `#c9303c` · `#b45309` |

`--ink-3` 밝은 값은 콘솔의 `#6b7684` 보다 한 단계 어둡다. `#f2f4f6`(staging 지면) 위에서 4.5:1 을 넘기려고 내렸다.

## 3. 서체 — Pretendard 한 벌

**계기 조판은 서체가 아니라 `tabular-nums slashed-zero` 로 만든다.**
고정폭(시스템 모노)은 코드 식별자 — API 경로 · 필드 이름 · 파일명 — 에만 쓴다. "기술적으로 보이려고" 쓰지 않는다.

| 역할 | 크기 | 행간 | 자간 | 굵기 |
|---|---|---|---|---|
| `.display` (히어로) | `clamp(2.3rem, 4.6vw, 3.9rem)` | 1.12 | −0.037em | 700 |
| `.close__title` | `clamp(2.05rem, 4.2vw, 3.3rem)` | 1.22 | −0.031em | 700 |
| `.h2` | `clamp(1.85rem, 3.6vw, 2.9rem)` | 1.18 | −0.032em | 680 |
| `.lede` | `clamp(1.06rem, 1.45vw, 1.24rem)` | 1.68 | −0.016em | 400, max 42ch |
| 본문 | 17px | 1.75 | — | 400, max 64ch |

`word-break: keep-all` — 한국어를 음절 단위로 끊지 않는다.
**행간은 라틴 기준으로 잡지 않는다.** 한글은 글자가 상자를 꽉 채워 디센더 여유가 없고, 행이 늘수록 필요한 행간도 는다.
**한글에 넓은 자간을 주지 않는다.** `.inst-label` 의 0.15em 대문자 자간은 영문 라벨 전용이다 — 한글 라벨은 0.01em.
레일의 세로쓰기는 뒤집지 않는다(`text-orientation: mixed`). 뒤집으면 한글이 거꾸로 선다.

## 4. 간격

`--s1 6 · --s2 10 · --s3 16 · --s4 24 · --s5 36 · --s6 56 · --s7 88 · --s8 132`. 하나의 스케일만 쓴다.
구간 상하 여백은 `--s8`(모바일 `--s7`), 머리와 본문 사이는 `--s7`.

## 5. 구조 문법

**카드가 아니라 규칙선과 행이 구조를 만든다.** 아이콘+제목+본문 카드 3단 그리드를 쓰지 않는다. 눈썹(kicker)도 쓰지 않는다.
상태 스탬프(`.stamp`)는 예외다. 순서(일생)가 정보 자체라서 상태 표기가 장식이 아니다.

| 컴포넌트 | 쓰는 자리 |
|---|---|
| `.rail` | 좌측 고정 상태 레일(모바일은 하단 바). 페이지의 척추 |
| `.readout` | 히어로의 계기 판독 — 실제 응답 필드 이름(`resolved_version` …) |
| `.gap` | 마주 놓는 두 자리 (흩어짐) |
| `.split` · `.tiers` | 두 제품 · 두 Tier 대비. 실패 행은 `--no` |
| `.parts` · `.mock` | 저장 구조와 콘솔 화면 목업. 목업은 콘솔 라이트 토큰 그대로 |
| `.catalog` | 공식 프롬프트 9개 원장 |
| `.vstack` · `.guards` | 버전 기록과 "어디서 막나" |
| `.classes` | 모델 클래스 표 — 받지 않는 파라미터는 `--no` |
| `.play` · `.check` · `.verdict` | 라벨 이동 판정기 |
| `.routes` · `.json` | 금지 경로 / 허용 경로 · 응답 |
| `.pipe` · `.step` | 분기 없는 실행 경로 |
| `.channels` | 배포 채널 — **채운 점은 있는 채널, 속이 빈 고리는 아직 없는 채널** |
| `.loop` | 평가가 다음 버전으로 돌아오는 길. 없는 단계는 점선 + "아직" |
| `.ledger` | 기획서 기준 × 코드 상태 원장. **안 만든 줄도 싣는다** |
| `.cannots` | 못 하는 것. 각주가 아니라 정식 구간이다 |

## 6. 모션 — authored moment 하나

**한 프롬프트의 일생이 지나가는 것**이 유일한 연출이다. 그 외에는 `[data-in]` 등장 한 가지
(14px 상승 + 페이드, 0.72s, `cubic-bezier(.16,1,.3,1)`)뿐이고, 형제끼리 55ms 씩만 어긋난다.

등장 연출은 `@media (scripting: enabled)` 안에만 산다. 자바스크립트가 없으면 관찰자가 못 돌아 본문이 영영 숨기 때문이다.
`prefers-reduced-motion` 이면 등장이 사라지고 별자리는 정적 배경(`.sky__fallback`)으로 대체된다.

## 7. 배경 별자리 (`app/lib/constellation.ts`)

| 구간 | 장면 |
|---|---|
| 복사본 · 흩어짐 | 점이 넓게 흩어져 떠다닌다. **셋 중 하나는 다른 점 바로 옆에 붙은 사본**이다 |
| draft | 점이 직군 6개(`Category`) 묶음으로 모이고 이웃끼리 선이 생긴다. 점마다 조금씩 늦게 모인다 |
| v1 ~ staging | 밝은 지면이 덮는다 — 렌더 루프 정지 |
| production | 공식 프롬프트 한 점이 커지고 트인 라벨 고리가 걸린다. 바깥에 배포 채널 6개가 뜬다 |
| run | 빛이 그 점에서 채널로 나간다 |
| eval | 빛이 채널에서 그 점으로 돌아온다 — 실행이 테스트케이스가 되어 돌아오는 길 |
| 아직 | 아직 없는 채널(빈 고리)이 천천히 깜박인다 |

- **구현 상태를 그린다.** 채널 6개 중 웹 콘솔 · 플레이그라운드만 채운 점이고, MCP · CLI · Slack · SDK 는 빈 고리다.
  빛도 채운 점으로만 흐른다. 채널을 만들면 `LIT_CHANNELS` 를 바꾼다. `.channels` 표의 점 모양과 같은 표기다.
- **점과 선은 같은 셰이더 조각(`MOTION`)으로 움직인다.** 따로 계산하면 선 끝이 점에서 떨어진다.
- **목표값을 따라간다.** 목차 링크로 멀리 건너뛰어도 순간이동하지 않게 단계 값을 지수 감쇠로 쫓는다.
- **넓은 화면에서는 오른쪽으로 비켜 둔다.** 제목과 겹치지 않게 — 라벨 고리가 닫는 말 제목을 가렸던 적이 있다.
- **안 쓸 것은 받지 않는다.** 움직임 줄이기 · WebGL2 없음은 `FlowScene` 이 동적 import **전에** 판정한다.
- **캔버스에 글자를 그리지 않는다.** 카피는 전부 DOM 이다.
- 결정적 난수(시드 고정) — 새로 고쳐도 같은 모양이다.

## 8. 판정기 (`app/lib/gates.ts`)

서버의 `move_label()` 순서를 그대로 따른다: **editor 권한 → (staging 이면 끝) → approver 권한 → 게이트 전부**.
권한에서 막히면 게이트는 "판정 안 함"으로 그린다. 서버도 거기서 멈춘다.
실행 기록이 없으면 assertion · Judge 회귀 줄을 서버가 만들지 않는다 — 여기서는 "판정 안 함"과 이유로 보여 준다.
실패 문구는 서버 원문(합니다체)이다. 페이지의 해요체와 말투가 다른 이유다.

기본 상태(`TODAY`)는 저장소의 실제 모습이다 — 테스트셋 화면이 없어 production 이 항상 막힌다.

## 9. 이 세계가 거부하는 것

- 눈썹(kicker) — 제목이 스스로 선다
- 아이콘+제목+본문 카드 3단 그리드
- 히어로-지표 템플릿(큰 숫자 + 작은 라벨 타일)
- 그라디언트 텍스트
- 기술적으로 보이려고 쓰는 고정폭
- 아이콘 자리를 대신하는 유니코드 글리프 · 이모지 (아이콘은 1.5 선 두께로 직접 그렸다)
- **저장소에 없는 수치 · 코드보다 앞서 나가는 문장.** 시연용 값은 시연용이라고 화면에 적는다
